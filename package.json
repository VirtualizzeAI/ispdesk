{
  "name": "ispdesk",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:front": "npm --prefix ./front run dev",
    "dev:back": "npm --prefix ./back run dev",
    "dev": "npx concurrently --names FRONT,BACK --prefix-colors cyan,yellow \"npm run dev:front\" \"npm run dev:back\"",
    "build": "npm --prefix ./front run build",
    "build:front": "npm --prefix ./front run build",
    "build:back": "npm --prefix ./back run check",
    "lint": "npm --prefix ./front run lint"
  }
}
